let questions = [{
    question1: "What is an array?",
    choices1: ["A special variable which can hold more than 1 value", "A variable that can store a single item of text", "A single number", "A way to create a proccess that will perform a task for you."]
}]

let button = document.querySelector(".btn btn-primary brn-lg")

button.addEventListener("click", function(event) {

})